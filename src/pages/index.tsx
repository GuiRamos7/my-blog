import { Text, Flex } from '@chakra-ui/react';
import Header from '../components/Header';
const Home = () => {
  return (
    <Flex h='100vh'>
      <Header />
    </Flex>
  );
};

export default Home;
